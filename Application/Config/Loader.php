<?php 

    defined( 'Q_APP' ) or die( 'คุณไม่มีสิทธิ์เข้าถึงไฟล์นี้โดยตรง' );

    /**
     * Config Loader
     * เป็นไฟล์ตั้งค่าสำหรับการดึงไฟล์ต่างๆ
     * โดยท่านสามารถเพิ่มหรือแก้ไขได้
     */

    /**
     * @var CORE_LOADER
     * ทุก Core ที่กำหนดใน Array จะถูกดึงอัตโนมัติเมื่อเริ่มทำงาน
     */
    define( 'CORE_LOADER' , array(
        // Cores
        
    ) );
    

    /**
     * @var HELPER_LOADER
     * ทุก Helper ที่กำหนดใน Array จะถูกดึงอัตโนมัติเมื่อเริ่มทำงาน
     */
    define( 'HELPER_LOADER' , array(
        // Helpers

    ) );


?>