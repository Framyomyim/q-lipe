<?php 

    defined( 'Q_APP' ) or die( 'คุณไม่มีสิทธิ์เข้าถึงไฟล์นี้โดยตรง' );

    /**
     * Config Route
     * เป็นไฟล์ตั้งค่าสำหรับการเรียกใช้ไฟล์หลัก
     */

    /**
     * @var DEFAULT_ROUTE
     * ไฟล์หลักที่มีให้ตั้งแต่เริ่มต้นคือ Home.php
     */
    define( 'DEFAULT_ROUTE' , 'Home' );

?>